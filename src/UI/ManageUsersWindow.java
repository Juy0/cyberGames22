package UI;

import javax.swing.*;

public class ManageUsersWindow extends JFrame {
    public ManageUsersWindow() {
        setTitle("Gestion des Utilisateurs");
        setSize(600, 400);
        setLocationRelativeTo(null);

        // Ajoutez ici des composants comme un JTable pour lister les utilisateurs
        // ou des boutons pour ajouter/modifier/supprimer des utilisateurs


    }
}